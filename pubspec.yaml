name: totem
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.7.0


dependencies:
  flutter:
    sdk: flutter

  go_router:
  flutter_bloc:
  provider:
  dio:
  pretty_dio_logger:
  flutter_secure_storage:
  uuid:
  either_dart:
  barcode_widget:
  socket_io_client: ^3.1.2
  rxdart:
  cached_network_image:
  equatable:
  image_picker:
  flutter_svg:
  google_fonts:
  # font_awesome_flutter: # ❌ REMOVIDO: Não estava sendo usado (~50 KiB economizados)
  url_launcher:
  brasil_fields:
  bot_toast:
  get_it: ^7.6.4
  injectable: ^2.3.2
  shared_preferences:
  url_strategy:
  eva_icons_flutter:
  # percent_indicator: ^3.4.0  # ❌ REMOVIDO: Não estava sendo usado
  google_sign_in:
  flutter_staggered_grid_view: ^0.7.0
  web: ^1.1.1
  firebase_core:
  firebase_auth:
  phone_numbers_parser: ^9.0.3
  mask_text_input_formatter: ^2.9.0
  intl:
  rating_summary:
  nb_utils:
  collection:
  # animated_text_kit: ^4.2.3  # ❌ REMOVIDO: Não estava sendo usado
  hydrated_bloc:
  path_provider:
  hive:
  hive_flutter:
  # mobile_scanner:  # ❌ REMOVIDO: Não estava sendo usado (~100 KiB economizados)
  flutter_dotenv: ^5.1.0
  shimmer:
  smooth_page_indicator: ^1.2.0+3  # ✅ ADICIONAR
  # video_player: ^2.8.2  # ❌ REMOVIDO: Não usamos mais vídeos no cardápio (~150 KiB economizados)
  geolocator: ^13.0.1
  share_plus: ^10.1.2  # ✅ NOVO: Compartilhamento de produtos
  flutter_map: ^7.0.2  # ✅ NOVO: Mapas (Android, iOS e WEB!) - Usa tiles do Mapbox
  latlong2: ^0.9.1  # ✅ NOVO: Coordenadas geográficas para flutter_map
  encrypt:
  crypto:
  coupon_uikit: ^0.2.1
  qr_flutter: ^4.1.0
  package_info_plus: ^9.0.0





dev_dependencies:
  flutter_test:
    sdk: flutter


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/images/
    - assets/icons/
    - assets/env



  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
